# 数据工程提升汇总

## 一、MicroLM 预训练数据

| 阶段 | Before | After | 提升 |
|------|------|------|------|
| 数据来源 | `torch.randint` 随机 token | MiniMind 127 万条真实中文 | loss 从 7.08→7.03 变为 8.92→3.50 |
| 清洗 | 无 | 4 条规则 (控制字符/HTML/空白/长度) + CLI 控制 | 过滤率 0.49%，可复现 |
| 去重 | 无 | SHA256 精确去重 | 去重 255 条 |
| 切分 | 无 | SHA1 确定性切分 (99:1) | 可复现 train/valid，无随机偏移 |
| 文档边界 | 无 | EOS 分隔符 | 模型感知文档边界 |
| 词表 | 随机生成 token | 自训 BPE 6400 | 3.3M embedding (vs 32000 词表需 16.4M) |
| Tokenizer 训练 | 全量 1.2GB OOM | 子集 5 万行训练 | 效果等价，内存够用 |
| 编码 | 无 | 3.1 亿 train tokens + memmap | 随机采样加载，无内存压力 |
| D/N 比 | ~2x (4000 steps) | ~43x (50000 steps) | loss 从 5.48 降到 3.50 |

## 二、Qwen InstructIE 数据

| 阶段 | Before | After | 提升 |
|------|------|------|------|
| 原始数据 | 171K 条，字段不统一 | 6 步 Pipeline 标准化 | 全量 JSON 通过合法性校验 |
| 字段一致性 | train 用 `text`，valid 用 `input` | 统一为 `input` | Step 1 标准化 |
| 跨集泄漏 | train∩valid 143 条 | 硬过滤剔除 | 评测数据无污染 |
| 空 relation | 2446 条 | 硬过滤剔除 | 训练无噪声样本 |
| 质量分层 | 无 | 匹配率/关系数/长度三维度打分 | high: 74.7%，仅取高质量 |
| 任务覆盖 | 单一 IE 抽取 | 4 类任务派生 (50/20/15/15) | 覆盖抽取/问答/核查/推理 |
| 采样策略 | 随机采样 | 分层采样 (task×topic×quality) | 12 个 topic 各 2500 条均衡 |
| SFT 数据格式 | MiniMind `### Human:` 协议 | ChatML `<\|im_start\|>` 标准格式 | 与 Qwen 推理格式一致 |
| 评测数据一致性 | eval 用写死的 prompt | 复用训练时期 ChatML 原始文本 | Parse% 从 72% 提升到 95.2% |
| Token 预算 | `max_new_tokens=512` 截断 | `max_new_tokens=1024` | Parse% +8pp |

## 三、跨项目数据方法论对比

| 维度 | MicroLM | Qwen | 交叉验证结论 |
|------|------|------|------|
| 数据规模 | 127 万条 | 171K → 28.5K | 规模 ≠ 质量，28.5K 高质量比 100K 噪声好 |
| 清洗策略 | 4 条规则统一处理 | 6 步差异化处理 | 通用数据用粗筛，任务数据用精筛 |
| 词表设计 | 6400 (省参数) | 152064 (标准) | 词表和模型规模正相关 |
| 格式设计 | 自定义 `### Human:` | ChatML 标准 | 学习用自定义，生产用标准 |
| 评测指标 | 人工评分 (主观) | Parse%/Strict% (客观) | 结构化任务用硬指标 |
| 采样策略 | 随机窗口 | 分层采样 | 多任务/多领域必须分层 |

## 四、面试最亮点的 5 个数据提升

| # | 提升 | 数字 | 面试怎么说 |
|------|------|------|------|
| 1 | 子集训练 BPE | 5 万/127 万 | "分析证明 5 万行中文已覆盖所有常见字符对，6400 词表不需要全量" |
| 2 | 6 步 Pipeline | 171K→28.5K | "每步独立可审计，质量分层只采 high，最终全量 JSON 校验 100%" |
| 3 | eval 格式一致性 | Parse% 72%→95.2% | "发现 eval 用的 system prompt 和训练不一致，修正后 Parse% +23pp" |
| 4 | token 预算优化 | 512→1024 | "截断导致 28% 的 Parse 失败，改为 1024 后全部通过" |
| 5 | 跨集泄漏检测 | 143+181 条 | "训练前 profiling 发现泄漏，硬过滤剔除后评测结果可信" |
