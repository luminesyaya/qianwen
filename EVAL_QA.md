# 评测模块 — 面试问答

## Q1: 你的评测体系是什么？为什么不用人工打分？

> MicroLM 用人工打分——8 条固定 prompt，四模型对比，评分 1-5。Qwen 用全自动评测——Parse%、Strict%、Alias-Strict% 三个硬指标。结构化输出的正确性是客观的——JSON 要么能解析要么不能，字段要么齐全要么缺失。人工打分在这里不仅不必要，还会引入主观偏差。

---

## Q2: Parse%、Strict%、Alias-Strict% 分别测什么？

| 指标 | 测什么 | 怎么算 |
|------|------|------|
| Parse% | 输出是不是合法 JSON | `json.loads(reply)` 是否成功 |
| Strict% | 结构是否和 gold 完全一致 | keys 相同 + 数组长度相同 |
| Alias-Strict% | 至少包含 entities + relations | 两个 key 都存在且为 list |

> Parse% 测基础格式能力，Strict% 测精确匹配，Alias% 测结构完整性。我的结果是 Parse% 95.2%、Strict% 23.8%、Alias% 95.2%。Strict% 低不是因为格式错——keys 完全一致——是因为模型抽取的实体数量和人标的不完全一致。

---

## Q3: Strict% 只有 23.8%，为什么这么低？

> 不是格式问题，是数量问题。pred keys 和 gold keys 完全一致（都是 `['entities', 'relations']`）。但同一个文本，人标了 5 个实体，模型可能抽了 4 个或 6 个——多一个少一个都不算完全一致。这是 IE 任务本身的天花板——人工标注的标准答案也不唯一。

---

## Q4: Parse% 第一次只有 72%，怎么 debug 到 95.2% 的？

> LoRA 训练完第一次跑 eval，Parse% 只有 72%。我先加了两行 debug 打印失败的原始回复，发现两个问题都不是模型的问题：
>
> **问题 1**：JSON 被截断。`max_new_tokens=512` 不够装完整 JSON，最后一个 `}` 没输出就停了。改成 1024 解决。
>
> **问题 2**：eval 用的 system prompt 是写死的，和训练时每条数据独有的 system 不一致。SFT 的训练-推理 prompt 必须完全一致——我从 ChatML 原始文本拆出训练时期的 system + user，一字不改喂给模型。
>
> 两个改动加起来，Parse% 从 72% 跳到 95.2%，涨了 23 个百分点。这段 debug 经历让我深刻理解了为什么训练-推理格式一致性是 SFT 的生命线。

---

## Q5: MicroLM 的评测结果是多少？

| 模型 | 平均评分 | 能力 |
|------|:--:|------|
| Pretrain | ~0.5 | 纯续写/乱码 |
| SFT 全参 | ~3.0 | 基本对话、编号列表 |
| LoRA (0.6%) | ~2.5 | 概念解释更强，格式相近 |
| DPO (模拟数据) | ~0.5 | "A A A" 崩溃 |

> SFT 使评分提升 ~600%，LoRA 以 0.6% 参数接近全参效果。DPO 因模拟数据无效而崩溃。MicroLM Parse% = 0%，这直接驱动了向 Qwen 迁移的决策。

---

## Q6: MicroLM 和 Qwen 的评测方式有什么不同？

| | MicroLM | Qwen |
|------|------|------|
| 方式 | 固定 prompt + 人工打分 | 全自动 Parse%/Strict%/Alias% |
| 样本量 | 8 条 prompt | 1500 条 valid |
| 主观性 | 有 | 零 |
| 可复现性 | 人工依赖 | 完全可复现 |

> 两个评测方式对应两个任务：通用聊天评测靠人工（因为"好"是主观的），结构化抽取评测靠硬指标（因为"对"是客观的）。哪个场景用哪种方式。

---

## Q7: 评测之外你还做了什么分析？

> 能力边界分析——不是只报指标，而是分析模型"能做到什么、做不好什么、做不了什么"。MicroLM 能简单问答、格式列表，做不好精确翻译，做不了 JSON 输出和长文一致。结构化质量分析——实体做 key 率、全中文字段率、字段名重叠率，量化 LoRA 学到的结构化行为塑形效果。Alias 归一化——构建近义词映射表归一化后再算 Strict%，但我的模型不需要——pred keys 和 gold 完全一致。

---

## Q8: 评测有什么局限性？

> 当前只评测了 ie_extraction 类型的样本。其他三类任务（relation_qa、entity_verification、reasoning）的输出是纯文本，没有自动 JSON 指标。如果要加，可以引入 ROUGE/BLEU 做文本质量评测，或人工抽检 50 条打分。另外，消融实验缺失——没法量化 Pipeline 每步的贡献（"不加质量分层 Parse% 降多少"回答不了）。这两个是待改进点。

---

## 面试一条线讲法

> "我的评测分两条线。MicroLM 用人工评分——8 条固定 prompt，四个模型横向对比，画了能力边界图。Qwen 用全自动评测——Parse%/Strict%/Alias% 三个硬指标，不需要人工干预。
>
> 第一次跑 Qwen eval Parse% 只有 72%。我没有接受这个数字——加 debug 打印看失败的原始输出，发现两个问题：JSON 截断（512 token 不够）和 system prompt 不一致（eval 写死和训练不同）。修完涨到 95.2%。这个 debug 过程让我深刻理解了 SFT 的训练-推理格式一致性有多重要。
>
> MicroLM Parse% = 0% 是最硬的证据——38M 小模型不具备 JSON 输出能力。这直接驱动了向 Qwen 迁移的决策。从 0 到 95.2%，这个对比是我项目最有价值的数据。"
